Wordsmith Studio V2 (standalone)

This directory contains the complete mobile-capable app and all runtime dependencies.
Serve this directory with any static HTTP server. No install or build step is required.
